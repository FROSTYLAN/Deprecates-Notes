const data = {
  frontend: "React",
  backend: "NodeJS",
  design: "Figma",
};

const entries = Object.entries(data);
console.log(entries);

const keys = Object.keys(data);
console.log(keys);

const values = Object.values(data);
console.log(values);
