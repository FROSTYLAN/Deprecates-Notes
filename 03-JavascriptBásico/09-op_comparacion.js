// Igual que
x == y;

// Estrictamente igual que
x === y;

// Mayor que
x > y;

// Mayor igual que
x >= y;

// Menor que
x < y;

// Menor igual que
x <= y;

// Diferente que
x != y;

// Estrictamente diferente que
x !== y;
